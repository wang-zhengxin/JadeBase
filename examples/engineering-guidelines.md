# JadeBase 研发规范

## 代码审查

所有代码合并前必须通过单元测试和安全扫描。普通变更至少需要一名审查者批准，高风险变更需要两名审查者批准，并由模块负责人确认回滚方案。

## 发布要求

生产发布必须关联变更单，包含影响范围、验证步骤和回滚方案。数据库结构变更需要先完成兼容性评估，禁止在同一版本中直接删除仍被旧代码读取的字段。

## 故障响应

一级故障应在五分钟内响应，十五分钟内建立协同群并指定现场负责人。恢复服务优先于定位全部根因，事后两个工作日内完成复盘。
