# Vite dev server proxy target. The browser still calls the same /api paths.
VITE_BACKEND_PROXY_TARGET=http://localhost:8080

# Optional direct API origin, for deployments without a reverse proxy.
# VITE_API_BASE_URL=http://localhost:8080
